diff --git a/src/network/networkHeader.js b/src/network/networkHeader.js
index 2005256..d561b3d 100644
--- a/src/network/networkHeader.js
+++ b/src/network/networkHeader.js
@@ -84,7 +84,7 @@ export default GObject.registerClass(class NetworkHeader extends Header {
         }
     }
     resetMaxWidths() {
-        this.maxWidths = [];
+        this.maxSpeedWidth = null;
         if (!Config.get_boolean('network-header-io'))
             return;
         if (!this.speed.get_stage())
@@ -245,26 +245,33 @@ export default GObject.registerClass(class NetworkHeader extends Header {
             this.speed.style = style;
             this.speed.queue_relayout();
             this.speedContainer.queue_relayout();
+            this.maxSpeedWidth = null;
         }
-        const speedWidth = this.speed.get_preferred_width(-1);
-        const width = speedWidth ? speedWidth[1] : 0;
-        this.maxWidths.push(width);
-        if (this.maxWidths.length > Utils.networkMonitor.updateFrequency * 30)
-            this.maxWidths.shift();
-        let max = Math.max(...this.maxWidths);
-        if (max === this.speedContainer.width)
+        if (this.maxSpeedWidth === null || this.maxSpeedWidth === undefined)
+            this.maxSpeedWidth = this.computeMaxSpeedWidth();
+        if (this.maxSpeedWidth === this.speedContainer.width)
             return;
-        if (max <= 0)
-            max = 1;
-        this.speedContainer.set_width(max);
+        this.speedContainer.set_width(this.maxSpeedWidth);
+    }
+    computeMaxSpeedWidth() {
+        const unit = Config.get_string('network-io-unit');
+        let maxFigures = Config.get_int('network-header-io-figures');
+        maxFigures = Math.max(1, Math.min(4, maxFigures));
+        const worstCase = Utils.formatBytesPerSecMaxLength(unit, maxFigures);
+        const text = this.ioLayout === 'horizontal' ? `${worstCase} | ${worstCase}` : worstCase;
+        const previousText = this.speed.text;
+        this.speed.text = text;
+        const preferredWidth = this.speed.get_preferred_width(-1);
+        this.speed.text = previousText;
+        return preferredWidth && preferredWidth[1] > 0 ? preferredWidth[1] : 1;
     }
     update() {
-        this.maxWidths = [];
+        this.maxSpeedWidth = null;
         this.updateGraph();
         this.updateSpeed();
     }
     redraw() {
-        this.maxWidths = [];
+        this.maxSpeedWidth = null;
         this.fixSpeedContainerStyle();
     }
     createTooltip() {
diff --git a/src/processor/processorHeader.js b/src/processor/processorHeader.js
index 4f4c691..81a8eef 100644
--- a/src/processor/processorHeader.js
+++ b/src/processor/processorHeader.js
@@ -275,11 +275,19 @@ export default GObject.registerClass(class ProcessorHeader extends Header {
         }
     }
     buildFrequency() {
+        this.maxFrequencyWidth = null;
         this.frequency = new St.Label({
             text: '- GHz',
             styleClass: 'astra-monitor-header-frequency',
             yAlign: Clutter.ActorAlign.CENTER,
+            xAlign: Clutter.ActorAlign.END,
         });
+        const resetMaxFrequencyWidth = () => {
+            this.maxFrequencyWidth = null;
+        };
+        Config.connect(this.frequency, 'changed::processor-header-frequency-figures', resetMaxFrequencyWidth.bind(this));
+        Config.connect(this.frequency, 'changed::headers-font-family', resetMaxFrequencyWidth.bind(this));
+        Config.connect(this.frequency, 'changed::headers-font-size', resetMaxFrequencyWidth.bind(this));
         const updateFrequencyMode = () => {
             let frequencyMode = Config.get_string('processor-header-frequency-mode');
             if (frequencyMode && !['average', 'max'].includes(frequencyMode)) {
@@ -326,6 +334,30 @@ export default GObject.registerClass(class ProcessorHeader extends Header {
         else if (this.frequencyMode === 'max') {
             this.frequency.text = Utils.formatFrequency(Math.max(...activeFrequencies) / 1000, 'GHz', figures, true);
         }
+        this.fixFrequencyWidth();
+    }
+    computeMaxFrequencyWidth() {
+        let figures = Config.get_int('processor-header-frequency-figures');
+        figures = Math.max(2, Math.min(4, figures));
+        // Worst case: '0' is the widest digit in most fonts, and the decimal
+        // point is always present (forceDecimals), e.g. '0.00 GHz' for 3 figures.
+        const worstCase = `0.${'0'.repeat(figures - 1)} GHz`;
+        const previousText = this.frequency.text;
+        this.frequency.text = worstCase;
+        const preferredWidth = this.frequency.get_preferred_width(-1);
+        this.frequency.text = previousText;
+        return preferredWidth && preferredWidth[1] > 0 ? preferredWidth[1] : null;
+    }
+    fixFrequencyWidth() {
+        if (!this.frequency.get_stage())
+            return;
+        if (this.maxFrequencyWidth === null || this.maxFrequencyWidth === undefined)
+            this.maxFrequencyWidth = this.computeMaxFrequencyWidth();
+        if (this.maxFrequencyWidth === null || this.maxFrequencyWidth === undefined)
+            return;
+        if (this.maxFrequencyWidth === this.frequency.width)
+            return;
+        this.frequency.set_width(this.maxFrequencyWidth);
     }
     update() {
         const perCoreBars = Config.get_boolean('processor-header-bars-core');
diff --git a/src/storage/storageHeader.js b/src/storage/storageHeader.js
index 9f3cd7a..95373b0 100644
--- a/src/storage/storageHeader.js
+++ b/src/storage/storageHeader.js
@@ -101,7 +101,7 @@ export default GObject.registerClass(class StorageHeader extends Header {
         }
     }
     resetMaxWidths() {
-        this.maxWidths = [];
+        this.maxSpeedWidth = null;
         if (!Config.get_boolean('storage-header-io'))
             return;
         if (!this.speed.get_stage())
@@ -404,21 +404,28 @@ export default GObject.registerClass(class StorageHeader extends Header {
             this.speed.style = style;
             this.speed.queue_relayout();
             this.speedContainer.queue_relayout();
+            this.maxSpeedWidth = null;
         }
-        const speedWidth = this.speed.get_preferred_width(-1);
-        const width = speedWidth ? speedWidth[1] : 0;
-        this.maxWidths.push(width);
-        if (this.maxWidths.length > Utils.storageMonitor.updateFrequency * 30)
-            this.maxWidths.shift();
-        let max = Math.max(...this.maxWidths);
-        if (max === this.speedContainer.width)
+        if (this.maxSpeedWidth === null || this.maxSpeedWidth === undefined)
+            this.maxSpeedWidth = this.computeMaxSpeedWidth();
+        if (this.maxSpeedWidth === this.speedContainer.width)
             return;
-        if (max <= 0)
-            max = 1;
-        this.speedContainer.set_width(max);
+        this.speedContainer.set_width(this.maxSpeedWidth);
+    }
+    computeMaxSpeedWidth() {
+        const unit = Config.get_string('storage-io-unit');
+        let maxFigures = Config.get_int('storage-header-io-figures');
+        maxFigures = Math.max(1, Math.min(4, maxFigures));
+        const worstCase = Utils.formatBytesPerSecMaxLength(unit, maxFigures);
+        const text = this.ioLayout === 'horizontal' ? `${worstCase} | ${worstCase}` : worstCase;
+        const previousText = this.speed.text;
+        this.speed.text = text;
+        const preferredWidth = this.speed.get_preferred_width(-1);
+        this.speed.text = previousText;
+        return preferredWidth && preferredWidth[1] > 0 ? preferredWidth[1] : 1;
     }
     update() {
-        this.maxWidths = [];
+        this.maxSpeedWidth = null;
         this.updateBars();
         this.updatePercentage();
         this.updateValue();
diff --git a/src/utils/utils.js b/src/utils/utils.js
index 5662312..8b19269 100644
--- a/src/utils/utils.js
+++ b/src/utils/utils.js
@@ -545,6 +545,14 @@ class Utils {
         }
         return `${result} ${Utils.unitMap[unit].labels[unitIndex]}`;
     }
+    static formatBytesPerSecMaxLength(unit, maxNumbers = 2) {
+        if (!Object.prototype.hasOwnProperty.call(Utils.unitMap, unit))
+            unit = 'kB/s';
+        maxNumbers = Math.max(1, maxNumbers);
+        const numberPart = maxNumbers === 1 ? '9' : '9.' + '9'.repeat(maxNumbers - 1);
+        const megaLabel = Utils.unitMap[unit].labels[2];
+        return `${numberPart} ${megaLabel}`;
+    }
     static formatBytes(bytes, unit = 'kB-KB', maxNumbers = 2) {
         if (!Object.prototype.hasOwnProperty.call(Utils.unit2Map, unit))
             unit = 'kB-KB';
